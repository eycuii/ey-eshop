server:
  port: 11010

spring:
  application:
    name: ey-eshop-user
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: dev
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        file-extension: yml
        extension-configs:
          - dataId: common.yml
            refresh: true