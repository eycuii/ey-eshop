server:
  port: 11000

spring:
  application:
    name: ey-eshop-gateway
  jackson:
    time-zone: GMT+8
    serialization:
      FAIL_ON_EMPTY_BEANS: false
    date-format: yyyy-MM-dd HH:mm:ss
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: dev